<div class="container center">
  <div class="row">
    <%= form_for [current_user, @link] do |f| %>
    <%= f.text_field :title, placeholder: "Title" %>
    <br>
    <%= f.text_field :url, placeholder: "URL" %>
    <br>
    <%= f.submit "Submit", class: "btn btn-primary btn-sm", method: :post %>
    <% end %><br>
    <h2>All Links</h2>
  </div>
  <div class="row center-table">
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>URL</th>
          <th>Status</th>
          <th>Modify</th>
        </tr>
      </thead>

      <tbody>
        <% @links.each do |link| %>
        <tr>
          <td><%= link.title %></td>
          <td><%= link.url %></td>
          <% if link.read == false %>
          <td>Unread</td>
          <td>
            <button type="button" class="btn btn-success" aria-label="Left Align">
              <span class="" aria-hidden="true">Mark as Read</span>
            </button>
          </td>
          <% else %>
          <td>Read</td>
          <td>
            <button type="button" class="btn btn-danger" aria-label="Left Align">
              <span class="" aria-hidden="true">Mark as Unread</span>
            </button>
          <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
